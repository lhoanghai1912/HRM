import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Details_Shift = () => {
  return (
    <View style={styles.container}>
      <Text>Details_Shift</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
});

export default Details_Shift;
