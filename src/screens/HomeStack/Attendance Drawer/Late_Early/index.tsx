import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Late_Early = () => {
  return (
    <View style={styles.container}>
      <Text>Late_Early</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
});

export default Late_Early;
