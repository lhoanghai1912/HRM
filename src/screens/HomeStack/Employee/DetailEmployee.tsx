import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const DetailEmployee = () => {
  return (
    <View style={styles.container}>
      <Text>DetailEmployee </Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
});

export default DetailEmployee;
